# Texture placeholders

Optional texture files can go here later.

The current simulator uses procedural geometry and materials only, which keeps the first version fast on mobile browsers and avoids loading external texture assets during startup.

Good future additions:

- Subtle console noise texture
- Transparent HUD glass texture
- Starfield variation maps
- Brushed metal panel texture
