# Assets

This folder is intentionally lightweight for the first milestone.

The app currently uses generated Web Audio placeholder sounds from `js/audio.js`, so it will run without audio files.

Later, real sound and texture files can be added here without changing the public interface of the app.
